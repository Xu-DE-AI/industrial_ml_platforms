apiVersion: apps/v1
kind: Deployment
metadata:
  name: geothermal-risk-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: geothermal-risk-api
  template:
    metadata:
      labels:
        app: geothermal-risk-api
    spec:
      containers:
        - name: api
          image: geothermal-risk-api:latest
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1"
              memory: "1Gi"
---
apiVersion: v1
kind: Service
metadata:
  name: geothermal-risk-api
spec:
  selector:
    app: geothermal-risk-api
  ports:
    - port: 80
      targetPort: 8000
